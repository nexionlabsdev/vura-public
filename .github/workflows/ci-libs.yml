name: CI - core-sdk / vura-odata-sync-core / vura-dataverse-sync-core / vura-io

# Foundation TS libraries — no native platform-specific deps of their own
# (vura-io's @duckdb/node-api aside, which the ubuntu runner covers), so
# ubuntu-only is enough. Path-filtered so an unrelated PR (e.g. touching only
# core-extension) doesn't run this.
on:
  push:
    branches: [main, personal-main]
    paths:
      - 'packages/core/core-sdk/**'
      - 'packages/core/vura-odata-sync-core/**'
      - 'packages/connectors/vura-dataverse-sync-core/**'
      - 'packages/core/vura-io/**'
      - 'schemas/**'
      - '.github/workflows/ci-libs.yml'
  pull_request:
    branches: [main, personal-main]
    paths:
      - 'packages/core/core-sdk/**'
      - 'packages/core/vura-odata-sync-core/**'
      - 'packages/connectors/vura-dataverse-sync-core/**'
      - 'packages/core/vura-io/**'
      - 'schemas/**'
      - '.github/workflows/ci-libs.yml'
  workflow_dispatch: {}

concurrency:
  group: ci-libs-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Update npm CLI
        run: npm install -g npm@latest

      - run: npm install --no-audit --no-fund

      - name: Build core-sdk
        run: npm run compile --workspace=packages/core/core-sdk

      - name: Build and test vura-odata-sync-core
        run: |
          npm run compile --workspace=packages/core/vura-odata-sync-core
          npm test --workspace=packages/core/vura-odata-sync-core

      # No test script defined on this package yet — build only.
      - name: Build vura-dataverse-sync-core
        run: npm run compile --workspace=packages/connectors/vura-dataverse-sync-core

      - name: Build and test vura-io (shredder, schema, and partitioning contracts)
        run: |
          npm run compile --workspace=packages/core/vura-io
          npm test --workspace=packages/core/vura-io
